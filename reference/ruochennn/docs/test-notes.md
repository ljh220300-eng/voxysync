# MapSyncer 功能测试文档

本文档用于记录 MapSyncer for XaeroWorldmap 模组的功能测试结果。

**测试环境要求**：
- Minecraft 1.21.X + NeoForge
- Xaero's World Map mod（最低版本 1.37.0）
- 服务端和客户端均安装 MapSyncer

---

## 一、服务端生成功能测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 全维度生成 | `/mapsyncer generate` | 生成所有已加载维度的地图缓存 | ✅ | |
| 单维度生成-主世界 | `/mapsyncer generate overworld` | 仅生成主世界缓存 | ✅ | |
| 单维度生成-地狱 | `/mapsyncer generate the_nether` | 仅生成地狱缓存（地表 + 默认 `SURFACE,63` 洞穴层） | ✅ | v1.0.4 修复基岩顶层与 Y=63 洞穴层 |
| 单维度生成-末地 | `/mapsyncer generate the_end` | 仅生成末地缓存 | ✅ | v3.2+v3.8已修复虚空渲染 |
| 单区域生成 | `/mapsyncer generate overworld 0 0` | 生成指定坐标的单个区域 | ✅ | |
| 强制生成 | `/mapsyncer generate overworld --force` | 强制重新生成，忽略缓存 | ✅ | |
| 生成状态查询 | `/mapsyncer status` | 显示生成进度+增量更新状态+下次更新时间 | ✅ | |
| Mod维度识别与生成 | `/mapsyncer generate twilightforest:twilight_forest` | 显示完整维度ID建议 + 成功生成 | ✅ | 暮光森林测试通过 |
| 维度路径格式 | 检查原版传统格式和Mod dimensions/格式 | 自动检测并正确使用 | ✅ | 原版：region/,DIM-1/,DIM1/；Mod：dimensions/<ns>/<path>/ |
| 首次转换写入配置 | 检查配置文件region_folder | 检测结果写入配置 | ✅ | |

---

## 二、客户端同步功能测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 单维度同步 | `/mapsyncer sync` 或 `/mapsyncer sync <dim>` | 同步指定维度 | ✅ | v4.1修复目录问题，v3.7修复冒号解析 |
| 同步所有维度 | `/mapsyncer sync all` | 同步所有可用维度 | ✅ | |
| 维度别名与建议 | `/mapsyncer sync nether` + 输入命令查看建议 | 支持别名 + 显示维度列表 | ✅ | v3.4统一格式，v3.6注册表支持 |
| Mod维度同步 | `/mapsyncer sync twilightforest:twilight_forest` | 同步Mod维度 | ✅ | v3.7修复greedyString解析 |
| 增量比对 | 同步后再次同步 + 服务端更新后同步 | 跳过无变化区域 + 仅同步变化区域 | ✅ | |
| 视野范围保护 | 在overworld时同步overworld，检查周围区域 | 卸载视野范围内region + 服务端数据覆盖 | ✅ | v4.6新增功能，同步前卸载视距内region |

---

## 三、洞穴模式功能测试（生成+同步）

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 洞穴模式配置 | 配置 layerPlan（如 `SURFACE,63`）+ 检查 `caves/<layer>/` 目录 | 配置生效，目录结构正确 | ✅ | 地狱默认 layer 3 (Y=63) |
| 洞穴层文件生成 | 检查 zip 文件命名 + 服务端生成日志 | 格式正确，生成成功 | ✅ | v1.0.4 |
| 洞穴层同步传输 | 同步地狱 + 检查客户端 `caves/<layer>/` 目录 | 文件传输 + 目录正确 | ✅ | v1.0.4 |
| 洞穴层显示 | Xaero 地图切换洞穴层 | 显示洞穴地形 | ✅ | v1.0.4 修复空像素/群系/缓存 |
| 洞穴+地表共存 | 同时生成地表和洞穴 | 两个目录都存在文件 | ✅ | `SURFACE,63` |

---

## 四、增量更新功能测试

| 测试项 | 测试命令 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| TICK模式启用 | `/mapsyncer incremental tick` | 按tick间隔自动扫描 | ✅ | v4.4修复配置保存 |
| SCHEDULED模式启用 | `/mapsyncer incremental scheduled` | 按指定时间自动更新 | ✅ | v4.4修复配置保存 |
| 增量模式禁用 | `/mapsyncer incremental off` | 停止自动更新 | ✅ | |
| 增量状态查询 | `/mapsyncer status` | 显示生成任务+增量模式状态+下次更新时间 | ✅ | 已与status合并 |
| 变化检测 | 生成后再次生成 + 修改区块后增量更新 | 跽过未变化区域，仅更新变化区域 | ✅ | |
| 配置文件同步 | 使用指令后检查配置文件 | 配置文件立即更新 | ✅ | v4.4已修复 |

---

## 五、配置文件测试

| 测试项 | 配置内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 服务端配置生成 | 首次运行生成配置文件 | 配置文件存在且格式正确 | ⬜ | |
| 配置修改生效 | 修改配置后重启服务器 | 新配置生效 | ⬜ | |
| 增量更新模式配置 | `incrementalUpdateMode = "TICK"` | TICK模式生效 | ⬜ | |
| 维度扫描配置 | 添加 dimension_configs + 设置 layerPlan | 配置生效 | ⬜ | 格式见 README |
| 客户端调试配置 | `enableDebugLogging=true` | 显示调试信息 | ⬜ | |
| 断点续传 | 同步中断后进服 | 提示 `/mapsyncer clearstate` 续传 | ⬜ | 由 `ClientTimestampCache.needsResume()` 驱动 |

---

## 六、文件系统测试

| 测试项 | 检查内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 服务端缓存目录 | server_map_cache/ + null/DIM-1/DIM1 | 目录自动创建 + Xaero格式命名 | ⬜ | |
| 服务端缓存文件 | mca_timestamps.cache + generation_cache.properties | 文件存在 + Properties格式 | ⬜ | |
| 客户端服务器目录 | Multiplayer_<server>/ | 正确识别服务器 | ⬜ | |
| 客户端World ID目录 | mw$<worldId>/ | 使用正确worldId | ⬜ | |
| 客户端时间戳缓存 | sync_timestamps.cache | 缓存文件存在 | ⬜ | |

---

## 七、网络与传输测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 传输进度显示 | 同步时聊天栏显示 | 百分比显示 + 区域数量 + 耗时 | ⬜ | |
| 分批传输 | 大区域同步 | 按配置分批，不卡顿 | ⬜ | 默认1MB分批 |
| 速率限制 | `syncSpeedLimitKBps=500` | 按限制传输 | ⬜ | |
| 断点续传 | 断线重连 + 跨维度 + 大地图中断 | 从中断位置继续 + 保留进度 | ⬜ | |
| 错误场景处理 | 无缓存 + 断开 + 服务重启 + 网络中断 | 正确提示或恢复 | ⬜ | |

---

## 八、兼容性与性能测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| Xaero集成 | Xaero's World Map正常工作 | 反射操作 + 缓存清理 + 选择性重载 | ⬜ | |
| C2ME兼容 | 安装C2ME优化mod | 主线程调度保存，不冲突 | ⬜ | |
| Chunky兼容 | 安装Chunky预生成mod | 预生成后正常同步 | ⬜ | |
| Mod维度兼容 | 安装自定义维度mod | 正常识别 + 正常生成 + 正常同步 | ⬜ | 暮光森林测试通过 |
| 版本向后兼容 | 新服务端+旧客户端 | 正常同步 | ⬜ | |
| 生成性能 | 大区域生成 + 并发数 | 合理时间完成 + 按配置并发 | ⬜ | 记录耗时 |
| 同步性能 | 哈希计算 + 传输速度 | 合理时间 + 按速率限制 | ⬜ | 并行度限制为2 |

---

## 九、边界情况测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 空维度处理 | 无区块的维度 | 正确处理，不崩溃 | ⬜ | |
| 负高度洞穴 | cave_start=-64 | caves/-4/目录正确 | ⬜ | |
| 超大坐标 | 远距离区域生成 | 正确处理 | ⬜ | |
| 空配置文件 | 删除配置后启动 | 自动重新生成 | ⬜ | |
| 单区块失败 | 模拟区块解析失败 | 不中断整体流程 + 记录错误 | ⬜ | |
| 磁盘空间不足 | 模拟磁盘满 | 提示错误，不崩溃 | ⬜ | |
| 无写入权限 | 模拟权限问题 | 提示错误，不崩溃 | ⬜ | |

---

## 十、渲染效果测试

| 测试项 | 测试内容 | 预期结果 | 测试结果 | 备注 |
|--------|----------|----------|----------|------|
| 主世界地形 | 主世界各种地形 | 地形正确显示 | ⬜ | |
| 地狱地形 | 地狱各种地形 | 地形正确显示 | ⬜ | 已知问题：区块竖线 |
| 末地地形 | 末地虚空+岛屿 | 虚空深紫色 + 岛屿正确 | ⬜ | v3.2+v3.8已修复 |
| 方块颜色 | 各种方块颜色 | 颜色基本正确 | ⬜ | |
| 生物群系边界 | 群系过渡区域 | 边界平滑 | ⬜ | MCA4x4x4精度限制 |
| 含水方块 | 水下方块、含水方块 | 正确渲染 | ⬜ | 已知差异 |
| 树木渲染 | 各种树木类型 | 正确渲染 | ⬜ | 已知不完整 |
| Mod方块颜色 | Mod添加的方块 | 启发式推断颜色 | ⬜ | 可能不准确 |

---

## 十一、测试总结

### 测试环境信息

| 项目 | 信息 |
|------|------|
| Minecraft 版本 | |
| NeoForge 版本 | |
| Xaero's World Map 版本 | |
| MapSyncer 版本 | |
| 服务端类型 | |
| 其他安装的 Mod | |

---

### 测试结果统计

| 类别 | 通过数 | 失败数 | 未测试数 | 备注 |
|------|--------|--------|----------|------|
| 服务端生成功能 | 9 | 0 | 1 | 地狱渲染差异待修复 |
| 客户端同步功能 | 7 | 0 | 0 | |
| 洞穴模式功能 | 0 | 0 | 5 | |
| 增量更新功能 | 6 | 0 | 0 | |
| 配置文件 | 0 | 0 | 6 | |
| 文件系统 | 0 | 0 | 5 | |
| 网络与传输 | 0 | 0 | 5 | |
| 兼容性与性能 | 0 | 0 | 7 | |
| 边界情况 | 0 | 0 | 7 | |
| 渲染效果 | 0 | 0 | 8 | |
| **总计** | **22** | **0** | **37** | |

---

### 已知问题汇总

| 问题 | 严重程度 | 状态 | 描述 |
|------|----------|------|------|
| 地狱渲染异常 | 中 | ⚠️ | 区块中间出现竖线分割 |
| 洞穴内容异常 | 低 | ⚠️ | 洞穴模式生成的文件存在一定异常 |
| 含水方块渲染 | 低 | ⚠️ | 水下方块、含水方块存在差异 |
| 树木渲染 | 低 | ⚠️ | 某些树木类型不完整 |
| Mod方块颜色 | 低 | ⚠️ | 启发式推断可能不准确 |

---

### 已修复问题汇总

| 问题 | 修复版本 | 描述 |
|------|----------|------|
| 末地渲染异常 | v3.2+v3.8 | 空白像素写入完整AIR方块状态，区块存在性检测 |
| 客户端同步目录识别 | v4.1 | getCurrentServerDirectory()方法，路径追踪 |
| 时间戳缓存路径 | v4.1 | lastBaseDir路径追踪，服务器切换自动重新初始化 |
| 缓存键格式不一致 | v4.1 | 服务端和客户端统一使用Xaero格式键 |
| Sync命令参数解析 | v3.7 | greedyString解析冒号，注册表推断维度ID |
| 增量更新配置保存 | v4.4 | incremental指令执行后自动调用saveConfig() |
| 维度路径格式 | v3.8 | 原版使用传统格式，Mod使用dimensions/新格式 |
| 聊天消息风格 | v4.2 | 统一[MapSyncer]前缀格式 |
| 代码重构清理 | v4.5 | 统一工具类，清理弃用方法，简化映射逻辑 |
| 视野范围保护 | v4.6 | 同步当前维度时跳过视野范围内区块，保留本地实时更新 |

---

### 测试完成确认

| 项目 | 状态 | 签名 | 日期 |
|------|------|------|------|
| 测试执行完成 | ⬜ | | |
| 问题记录完成 | ⬜ | | |
| 文档更新完成 | ✅ | | |

---

**测试文档版本**: 3.0
**最后更新**: 2026-05-21

---

# 指令驱动测试流程

本部分采用指令驱动测试方式，用户输入 `/mapsyncer` 指令后，按流程逐个检查每个环节是否符合预期。

---

## 一、`/mapsyncer generate` 指令测试流程

### 测试场景：全维度生成

**输入指令**: `/mapsyncer generate`

#### 环节 1：指令解析与反馈

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 指令识别 | 输入指令后观察聊天栏 | 显示 "[MapSyncer] 开始生成地图缓存..." | 通过 | |
| 参数解析 | 检查日志输出 | 无参数时默认生成所有维度 | 通过 | |
| 维度列表识别 | 检查日志中的维度列表 | 显示所有已加载维度的完整ID | 通过 | |

#### 环节 2：维度路径检测

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 原版维度路径 | 检查日志/配置文件 | overworld → `region/`<br>nether → `DIM-1/region/`<br>end → `DIM1/region/` | 通过 | |
| Mod维度路径 | 检查日志/配置文件 | 格式为 `dimensions/<namespace>/<path>/region/` |通过  | |
| 路径自动检测 | 检查服务端日志 | 显示检测到的路径格式 | 通过 | |


#### 环节 4：地图渲染生成

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 渲染线程启动 | 检查服务端日志 | 显示并发渲染线程数 | 通过 | |
| 地表渲染完成 | 检查生成的region文件 | 文件存在于 `server_map_cache/` | 通过 | |
| 洞穴层渲染 | 检查 caves 目录 | 如配置洞穴模式，caves/<layer>/ 存在文件 | ⬜ | |
| 时间戳记录 | 检查 `mca_timestamps.cache` | 文件存在且格式正确 | 通过 | |

#### 环节 5：生成完成反馈

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 完成消息 | 观察聊天栏 | 显示 "[MapSyncer] 生成完成，共 X 个区域" | 通过 | 为全量更新提供新提示|
| 生成统计 | 检查日志 | 显示各维度生成数量、耗时 | 通过 | 不需要显示耗时 |
| 缓存文件完整性 | 检查 `generation_cache.properties` | 记录所有已生成区域 | 通过 | |

---

### 测试场景：单维度生成

**输入指令**: `/mapsyncer generate <dimension>`

#### 环节 1：维度参数识别

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 维度别名支持 | 输入 `overworld`/`nether`/`end` | 正确识别并转换为完整ID | 通过 | |
| 完整维度ID | 输入 `minecraft:overworld` | 正确识别 | 通过 | |
| Mod维度ID | 输入 `twilightforest:twilight_forest` | 显示建议并正确识别 | ⬜ | |
| 无效维度处理 | 输入无效维度名 | 显示错误提示+可用维度列表 | ⬜ | |

#### 环节 2-5：同上全维度生成流程

按上述环节 2-5 检查，仅针对指定维度。

---

### 测试场景：单区域生成

**输入指令**: `/mapsyncer generate <dimension> <x> <z>`

#### 环节 1：坐标参数解析

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 坐标识别 | 输入区块坐标 | 显示 "生成区域 (x, z)" | ⬜ | |
| 区域计算 | 检查日志 | 正确计算 region 文件名 (r.X.Z.mca) | ⬜ | |

#### 环节 2-5：同上流程，仅生成单个区域

---

### 测试场景：强制重新生成

**输入指令**: `/mapsyncer generate <dimension> force`

#### 环节 1：强制参数识别

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| force 参数 | 检查日志 | 显示 "强制模式，忽略缓存" | ⬜ | 通过|
| 缓存忽略 | 检查时间戳比对 | 不比对时间戳，直接重新生成 | ⬜ |通过 |

#### 环节 2-5：同上流程，跳过增量检测环节

---

## 二、`/mapsyncer sync` 指令测试流程

### 测试场景：单维度同步

**输入指令**: `/mapsyncer sync <dimension>` 或 `/mapsyncer sync`

#### 环节 1：客户端准备检查

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 服务端连接状态 | 检查客户端状态 | 已连接服务端且服务端有缓存 | ⬜ | |
| 服务端缓存存在 | 服务端执行 `/mapsyncer status` | 显示缓存状态 | ⬜ | |
| 维度参数识别 | 输入指令后观察反馈 | 显示维度ID或默认当前维度 | ⬜ | |




#### 环节 4：数据传输

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 传输开始 | 观察聊天栏 | 显示 "[MapSyncer] 开始同步...共 X 个区域" | ⬜ | |
| 分批传输 | 观察进度更新 | 每批传输后更新百分比 | ⬜ | |
| 洞穴层传输 | 检查 caves 目录 | 如有洞穴层，同步 caves/<layer>/ | ⬜ | |
| 速率限制生效 | 检查网络流量 | 按配置速率限制传输 | ⬜ | |


---

### 测试场景：同步所有维度

**输入指令**: `/mapsyncer sync all`

#### 环节 1：维度列表获取

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 服务端维度列表 | 检查聊天栏反馈 | 显示可用维度列表 | ⬜ | |
| 客户端维度列表 | 检查客户端日志 | 记录待同步维度 | ⬜ | |

#### 环节 2-7：对每个维度依次执行单维度同步流程

---

## 三、`/mapsyncer status` 指令测试流程

**输入指令**: `/mapsyncer status`

#### 环节 2：缓存统计展示

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 维度缓存数量 | 观察聊天栏输出 | 显示各维度缓存区域数量 | ⬜ | |
| 总缓存大小 | 观察聊天栏输出 | 显示缓存总大小 | ⬜ | |

---

## 四、`/mapsyncer incremental` 指令测试流程

### 测试场景：TICK 模式启用

**输入指令**: `/mapsyncer incremental tick`

---

### 测试场景：SCHEDULED 模式启用

**输入指令**: `/mapsyncer incremental scheduled`

#### 环节 1：模式切换

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 模式切换反馈 | 观察聊天栏 | 显示 "[MapSyncer] 增量更新模式已切换为 SCHEDULED" | ⬜ | |
| 配置文件更新 | 检查配置文件 | `incrementalUpdateMode = "SCHEDULED"` | ⬜ | |

#### 环节 2：SCHEDULED 模式运行验证

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 定时任务启动 | 检查服务端日志 | 显示下次更新时间 | ⬜ | |
| 定时触发执行 | 等待触发时间 | 自动执行增量扫描 | ⬜ | |

---

### 测试场景：禁用增量更新

**输入指令**: `/mapsyncer incremental off`

#### 环节 1：模式禁用

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 禁用反馈 | 观察聊天栏 | 显示 "[MapSyncer] 增量更新已禁用" | ⬜ | |
| 配置文件更新 | 检查配置文件 | `incrementalUpdateMode = "OFF"` | ⬜ | |
| 定时任务停止 | 检查服务端日志 | 无增量扫描日志 | ⬜ | |

---

## 五、跨维度与断点续传测试流程

### 测试场景：跨维度同步中断恢复

**测试步骤**：
1. 执行 `/mapsyncer sync all`
2. 在传输过程中切换维度或断开连接
3. 重连后再次执行 `/mapsyncer sync <dimension>`

#### 环节 1：中断状态保存

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 进度保存 | 检查客户端缓存 | sync_progress.cache 记录中断位置 | ⬜ | |
| 已传输文件保留 | 检查客户端目录 | 已接收文件保留 | ⬜ | |

#### 环节 2：续传恢复

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 续传识别 | 检查客户端日志 | 显示 "从断点续传..." | ⬜ | |
| 已传输区域跳过 | 检查传输进度 | 跳过已接收区域 | ⬜ | |
| 续传完成 | 观察聊天栏 | 显示同步完成 | ⬜ | |

---

## 六、洞穴模式完整测试流程

### 测试场景：洞穴模式生成与同步

**前置配置**：
- 配置 `caveMode = "CAVE"`
- 配置 `caveStartHeight = 63`

#### 环节 1：洞穴配置验证

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 配置生效 | 检查配置文件 | 洞穴配置项正确设置 | ⬜ | |
| 层数计算 | 检查日志 | 显示洞穴层数计算结果 | ⬜ | |

#### 环节 2：洞穴层生成

**执行**: `/mapsyncer generate overworld`

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 地表层生成 | 检查 `server_map_cache/null/` | 地表 PNG 文件存在 | ⬜ | |
| 洞穴层目录创建 | 检查 `server_map_cache/null/caves/` | caves 目录存在 | ⬜ | |
| 洞穴层文件命名 | 检查 caves 目录 | 文件命名格式正确 | ⬜ | |
| 洞穴层渲染内容 | 打开 PNG 文件 | 显示洞穴地形 | ⬜ | |

#### 环节 3：洞穴层同步

**执行**: `/mapsyncer sync overworld`

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 洞穴层传输 | 检查客户端日志 | 显示洞穴层传输进度 | ⬜ | |
| 客户端洞穴目录 | 检查客户端 caves 目录 | 目录结构正确 | ⬜ | |
| 洞穴层显示 | Xaero 切换洞穴层 | 正确显示洞穴地形 | ⬜ | |

---

## 七、边界情况与错误处理测试流程

### 测试场景：无缓存同步

**执行**: 服务端无缓存时执行 `/mapsyncer sync overworld`

#### 环节检查

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 错误提示 | 观察聊天栏 | 显示 "服务端无缓存，请先生成" | ⬜ | |
| 命令处理 | 检查客户端状态 | 不崩溃，正常返回 | ⬜ | |

---

### 测试场景：空维度处理

**执行**: `/mapsyncer generate <empty_dimension>`

#### 环节检查

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 空维度检测 | 检查服务端日志 | 显示 "维度无区块数据" | ⬜ | |
| 命令处理 | 检查服务端状态 | 不崩溃，正常返回 | ⬜ | |

---

### 测试场景：网络中断恢复

**测试步骤**：
1. 执行同步命令
2. 模拟网络中断
3. 网络恢复后检查状态

#### 环节检查

| 检查项 | 检查方法 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|----------|------|
| 中断处理 | 检查客户端状态 | 不崩溃，显示连接错误 | ⬜ | |
| 断点保存 | 检查续传缓存 | 进度已保存 | ⬜ | |
| 重连恢复 | 重连后执行同步 | 从断点继续 | ⬜ | |

---

## 八、指令驱动测试记录表

### 测试执行记录

| 指令 | 执行时间 | 测试人员 | 主要环节结果 | 问题记录 |
|------|----------|----------|---------------|----------|
| `/mapsyncer generate` | | | ⬜ | |
| `/mapsyncer generate overworld` | | | ⬜ | |
| `/mapsyncer generate nether` | | | ⬜ | |
| `/mapsyncer generate the_end` | | | ⬜ | |
| `/mapsyncer generate <mod_dim>` | | | ⬜ | |
| `/mapsyncer generate overworld 0 0` | | | ⬜ | |
| `/mapsyncer generate overworld force` | | | ⬜ | |
| `/mapsyncer sync` | | | ⬜ | |
| `/mapsyncer sync all` | | | ⬜ | |
| `/mapsyncer sync overworld` | | | ⬜ | |
| `/mapsyncer sync nether` | | | ⬜ | |
| `/mapsyncer status` | | | ⬜ | |
| `/mapsyncer incremental tick` | | | ⬜ | |
| `/mapsyncer incremental scheduled` | | | ⬜ | |
| `/mapsyncer incremental off` | | | ⬜ | |
| 跨维度中断恢复 | | | ⬜ | |
| 洞穴模式完整流程 | | | ⬜ | |

---

### 测试环境信息

| 项目 | 信息 |
|------|------|
| Minecraft 版本 | |
| NeoForge 版本 | |
| Xaero's World Map 版本 | |
| MapSyncer 版本 | |
| 服务端类型 | |
| 其他安装的 Mod | |

---

### 测试完成确认

| 项目 | 状态 | 签名 | 日期 |
|------|------|------|------|
| 指令驱动测试执行完成 | ⬜ | | |
| 环节检查记录完成 | ⬜ | | |
| 问题记录完成 | ⬜ | | |
| 文档更新完成 | ✅ | | |

---

**测试文档版本**: 4.0（新增指令驱动测试流程）
**最后更新**: 2026-05-21
