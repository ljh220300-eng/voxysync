# Gradle 内存设置（仅构建环境，与 MC 服务端 JVM 无关）
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric / Loom
minecraft_version=1.20.1
loader_version=0.18.5
loom_version=1.14.6

# Mod
mod_id=voxysync
mod_name=VoxySync
mod_version=0.1.0
maven_group=com.nexus
mod_authors=Nexus, Jiemoy
mod_description=Server-pregenerated MCA region sync for Voxy LoD (full-map / radius modes, GPL-3.0), for MC 1.20.1

# Dependencies
fabric_api_version=0.92.12+1.20.1
